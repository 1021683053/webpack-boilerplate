<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Webpack Express Dev</title>
    {{if chunks.styles && chunks.styles.length>0}}
        {{each chunks.styles}}
    <link rel="stylesheet" href="{{publicPath}}{{@ $value}}" />
        {{/each}}
    {{/if}}
</head>
<body>
    <h2>Webpack Express Dev!</h2>
    {{if chunks.scripts && chunks.scripts.length>0}}
        {{each chunks.scripts}}
    <script src="{{publicPath}}{{@ $value}}"></script>
        {{/each}}
    {{/if}}
</body>
</html>