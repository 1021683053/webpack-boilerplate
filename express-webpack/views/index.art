<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Webpack Express Dev</title>
    {{if chunk.style && chunk.style.length>0}}
        {{each chunk.style}}
    {{@ $value}}
        {{/each}}
    {{/if}}
</head>
<body>
    <h2>Webpack Express Dev!</h2>
    {{if chunk.script && chunk.script.length>0}}
        {{each chunk.script}}
    {{@ $value}}
        {{/each}}
    {{/if}}
</body>
</html>